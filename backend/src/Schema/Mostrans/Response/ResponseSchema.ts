export const types = `
    type Response {
        status: String
        errMsg: String
        id_affected: String
    }

    type ResponseInsert {
        status: String
        errMsg: String
        id_affected: String
        return_id: String
    }
`;

export const inputs = ``;
export const queries = ``;
export const mutations = ``;
